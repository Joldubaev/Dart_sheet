/*

  Переменные
  Variables

 */
var name = 'Bob';
/*Variables store references. The variable called name contains a reference to a String object with a value of “Bob” */
Object name1 = 'Bob';
String name2 = 'Bob';

/*
A variable is “a named space in the memory” that stores values. 
In other words, it acts a container for values in a program. V
ariable names are called identifiers. 
Following are the naming rules for an identifier −
Identifiers cannot be keywords.
Identifiers can contain alphabets and numbers.
Identifiers cannot contain spaces and special characters, except the underscore (_) and the dollar ($) sign.
Variable names cannot begin with a number.
*/

var name7 = "Someone name";
Object name9 = "object isn’t restricted to a single type";
String name3 = "Type is string";
var name4 = 'Voyager I';
var year = 1977;
var antennaDiameter = 3.7;
var flybyObjects = ['Jupiter', 'Saturn', 'Uranus', 'Neptune'];
var image = {
  'tags': ['saturn'],
  'url': '//path/to/saturn.jpg'
};


/*
Variable is a named space in memory that stores value. 
You can say that it is a container for data.
*/

/*
Default value
Значение по умолчанию
Неинициализированные переменные,
 которые имеют тип, допускающий значение NULL, 
 имеют начальное значение null. Даже переменные 
 с числовыми типами изначально имеют значение null, 
 потому что числа, как и все остальное в Dart, являются объектами.

 int? lineCount;
assert(lineCount == null);
*/

